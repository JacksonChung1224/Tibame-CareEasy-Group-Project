# CareEasy 優化執行 — 進度與待辦總表
> 更新：2026-07-07（週二）｜對照文件：《CareEasy_優化執行規格書_v1.md》
> 里程碑：M2 真部署 **7/8（明天）** → M3 零重大 bug **7/15** → 決賽 **7/22**

---

## 一、已完成 ✅（本兩輪產出，均經沙箱實測）

| 任務卡 | 交付檔案 | 驗證狀態 | 待辦後續 |
|---|---|---|---|
| **P1-2** 訊號規則引擎 | `signalEngine.js` + `signalEngine.test.mjs` | ✅ 測試 9/9 通過 | ⚠️ Specialist 驗證 R1–R4 閾值與關鍵字表後才可 merge |
| **P2-2** OCR 後處理校正層 | `ocrPostprocess.js` + `ocrPostprocess.test.mjs` | ✅ 測試 12/12 通過 | 無 blocking；待 P2-1 UI 接線 |
| **P0-3** 費率合規清單（文件產出） | `rate-verification-checklist.md` | ✅ 6 項齊全（含新發現第 2 項「按包別部分負擔」） | 🔴 **Specialist 7/12 前完成勾選（blocking）** |
| **P0-2** 邀請碼連動 | `FamilyDiaryV3.jsx` | ✅ esbuild 語法驗證通過 | 邀請碼目前任意 6 碼即通過（demo）；正式版接 Supabase 驗證 |
| **P1-1** 快速標籤 chips | 同上（整合於 V3） | ✅ 舊制字串相容已由引擎測試覆蓋 | 無 |
| **P1-3** Solo 預估額度 tab | 同上（整合於 V3） | ✅ 免責文案含「最高」+「須經各縣市長期照顧管理中心評估核定」 | 「重新試算」按鈕待接試算平台路由 |

**煙霧測試結論（demo 敘事驗證）**：
- Connected：行走/移位🔴兩端一致 + 吞嚥/進食🔴兩端一致 + 血壓趨勢🟡 → 重評卡顯示 ✓
- Solo：同一份日誌 → 兩個🟡單邊訊號、無重評卡 → **連動前後對照即是「為什麼要連動」的 demo 論證**
- 已修正原型內部不一致：舊 `AI_SIGNALS` 引用不存在的 6/18 居服紀錄 → 補齊 6/18 mock、hardcode 全數移除

---

## 二、剩餘任務卡 ⬜（依優先序）

### 🔴 立即（本週，7/8–7/12）
- [ ] **P0-3 勾選**（負責：CMS Rules Specialist）— 6 項逐條對《長期照顧服務申請及給付辦法》
      與附表四查證。**完成前額度 tab / QuotaPanel 不得對外展示實際金額。**
- [ ] **V3 整合進 repo**（負責：Jackson）— `signalEngine.js` 放入 `src/utils/`、
      `FamilyDiaryV3.jsx` 調整 import 路徑，跑一次 `node signalEngine.test.mjs` 確認環境無誤。
- [ ] **M2 真部署驗證**（7/8）— V3 併入前先以現行版本達標，V3 於 M2 後合入，降低部署日風險。

### 🟡 下週前（7/13–7/15，M3 前）
- [ ] **P0-1 Demo 腳本**（可交低階模型，任務卡自包含）— 六幕 5 分鐘腳本 + 逐字口白。
      前置：無。備註：第五幕的「solo→connected 訊號升級對照」建議寫入口白（見上方煙霧測試結論）。
- [ ] **回歸測試**— V3 合入後全站點測：三入口、試算流程、日誌 CRUD、連動/解除、tab 切換。

### 🟢 決賽前（7/16–7/21，demo 環境即可）
- [ ] **P2-1 OCR Human-in-the-loop 確認流程**（可交低階模型）— 機構端插入步驟 2.5；
      前置：`ocrPostprocess.js` 已就緒，直接引用其 confidence 輸出。
- [ ] **P2-3 機構端差異標記規則表**（可交低階模型 + Specialist 驗證 D3/D4「以紙本為準」原則）
- [ ] **OCR 辨識引擎接線**（負責：Jackson，不宜交低階模型）— Claude API vision +
      結構化 JSON（逐欄 value + confidence）→ 串 `ocrPostprocess.js` 二次校正。
- [ ] **10 張手寫測試照片集**（負責：全隊皆可）— 手寫模擬居服紀錄拍照，
      統計「欄位級準確率」作為簡報數據（不要報整張全對率）。
- [ ] **Demo 排練 ×3 + 備援影片錄製**（7/20–7/21）

---

## 三、優化方向 Backlog 💡（非本期承諾，決賽後或行有餘力再排）

| # | 方向 | 說明 | 觸發條件 |
|---|---|---|---|
| B1 | **常數收斂單一來源** | V3 內 `BA_MAP`/`CMS_BUDGET`/`WELFARE_RATE` 與 `careData.js` 重複，P0-3 完成後統一 import 自 `careData.js`（對應清單第 2 項備註） | P0-3 勾選完成後 |
| B2 | **部分負擔按包別分流** | QuotaPanel 目前以單一 16% 計所有支出；若額度明細未來納入 D 碼交通（21–30%）/ EF 碼輔具（30%），計算需按碼別分流 | 明細出現非 BA 碼時（blocking 該功能） |
| B3 | **訊號 snapshot 進 Supabase** | 仿 `actual_cms_level` 思路：每次產出訊號時存快照，累積「訊號 → 實際重評結果」配對後回測 R1–R4 閾值（延續 backtest.mjs 方法論） | 有真實用戶資料後 |
| B4 | **重評文案定稿** | V3 已預改為行動引導版（「聯繫照管專員…討論是否申請提前複評（須經照管中心評估）」），待 P0-3 第 6 項確認後定稿 | P0-3 第 6 項勾選後 |
| B5 | **就醫摘要 PDF 匯出** | 「匯出 PDF」按鈕目前為佔位；A4 單頁、醫師 30 秒可讀版型（訊號 evidence 直接複用） | 決賽後 |
| B6 | **機構端整合進 Next.js** | Alpine.js 工具維持獨立至決賽；之後評估改寫或以 iframe/子路由長期共存 | 決賽後 |
| B7 | **邀請碼正式機制** | Supabase `invites` 表（code、institution_id、case_id、expires_at）+ RLS；機構端產碼 UI | 決賽後（demo 版任意 6 碼即可） |
| B8 | **CHANGELOG 記錄** | 本批變更（引擎 ×2、V3、費率驗證）依 Keep a Changelog 格式補記，費率驗證結果記入「💰 補助費率」區塊 | V3 合入 repo 時 |

---

## 四、風險看板 ⚠️

1. **P0-3 是唯一 blocking 路徑**：若 Specialist 7/12 未完成，額度相關畫面 7/15 M3 需以
   「僅顯示等級、隱藏金額」的降級版通過驗收。
2. **明天就是 M2**：V3 不要趕在部署日合入；先讓現行版本過 M2，V3 走 7/9–7/11 的窗口。
3. **訊號引擎的產品定位**：所有輸出堅持「觀察彙整、非醫療診斷」框架
   （UI 已有免責句），簡報口徑同步——這是評審必問的合規題。
